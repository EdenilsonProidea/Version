#LOADVAR INICIADORTMP INICIADOR AFCOS
#MSG $(INICIADORTMP)
#LET DF=5
#LET PF={$(XA)+4}
#LET POSIX={$(PL)+4.5} // 4.5 É A METADE D TUBO
#LET POSIY={$(XX8:0)+$(XX8:4)+277}
#MSG $(XX8:0)+$(XX8:4)+277

2	FF	X=$(POSIX) Y=$(PP) DF=$(DF) PF=$(PF)
2	FF	X={$(XL)-$(POSX)} Y=$(PP) DF=$(DF) PF=$(PF)
#IF $(DL)>=784
   2	FF	X={($(XL)/2} Y=$(PP) DF=$(DF) PF=$(PF)
#ENDIF