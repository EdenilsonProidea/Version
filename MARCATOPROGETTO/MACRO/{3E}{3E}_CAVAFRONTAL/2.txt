#IF $(_FR:6)=111
   // A cava é desenhada na gestão da porta, feito em visita técnica
#ELSE
   #IF $(DESATIVACAVAVAO:0)=0
      #IF $(cava)=1 & $(tpf)>0 & $(_TPCAIXA:9)[1;3]!0
         #IF $(SPOSTAA)+$A<1500
            _INICIA_AFRE_CAVA
            #IF $(canto)=0
               cava,l=$(lcava)
            #ENDIF
         #ENDIF
      #ENDIF
      #IF $(_puxcava:3)>0
         #LET afre=$(afre)-$(_puxcava:4)
         #LET affre=$(affre)+$(_puxcava:4)
      #ENDIF
   #ENDIF
#ENDIF