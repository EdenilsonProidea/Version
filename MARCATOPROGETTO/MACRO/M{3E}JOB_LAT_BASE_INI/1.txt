#M JOB_INICIA_TPFIX
#IF $(pl)>0 #LET f=2 #LET f=1
#IF $(sx1)>0  & $(ay)!0
   #LET pp=$(sx1)
#ENDIF
#IF ($(sx2)>0 & $(dp)>{$(xp)+150}) #LET dp=$(sx2)]
$(PP)+$(DP)>$(XP)
#IF $(PP)+$(DP)>$(XP) #LET DP={$(XP)-$(PP)}