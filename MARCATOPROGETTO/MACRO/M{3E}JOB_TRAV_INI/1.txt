#M JOB_INICIA_TPFIX
#IF $(pa)<-100 #LET f=1 #LET f=2
#IF $(TIPOLOGIAP)!220.002 & $(TIPOLOGIAP)!310
   #IF $(PP)+$(XP)>$(DP) #LET XP=$(DP)-$(PP)
#ENDIF
#IF $(PP)<0
   #LETS F1=5
   // #LET DISTX=0
#ELSE
   #LETS F1=6
   //  #LET DISTX=$(XL)
#ENDIF