#IF $(distanciadoresq:0)>0
   #IF $(_TPFECH:3)=1 // Adicionado em visita
      385 // L={$(distanciadoresq:0)+$(ESPARM:3)/2},sp=$p comentado em visita // Distanciador lateral Fechamento
   #ELSE
      385,sp=$p // L={$(distanciadoresq:0)+$(ESPARM:3)/2} comentado em visita // Distanciador lateral Fechamento
   #ENDIF
#ENDIF
// 201
// 204